##Styleguide

 * Clone repo
 * make sure you have styleguide installed globally: `sudo npm i -g devbridge-styleguide`
 * run `npm i`
 * start with `gulp` and navigate to **localhost:3000/library/**

Create snippets etc using the on-page controls.

Edit styles in the `scss` folder